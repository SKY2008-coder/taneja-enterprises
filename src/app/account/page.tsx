import Link from "next/link";
import { redirect } from "next/navigation";
import { createServerSupabaseClient } from "@/lib/supabase/server";
import { StorefrontHeader } from "@/components/storefront";
export default async function AccountPage() { const supabase = await createServerSupabaseClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login?next=/account"); return <><StorefrontHeader /><main className="mx-auto max-w-4xl px-4 py-8"><p className="text-sm text-slate-500">Signed in as</p><h1 className="text-3xl font-black">{user.email}</h1><div className="mt-8 grid gap-4 sm:grid-cols-2"><Link href="/account/profile" className="rounded-2xl border bg-white p-6 font-bold">Profile</Link><Link href="/account/addresses" className="rounded-2xl border bg-white p-6 font-bold">Addresses</Link><Link href="/account/orders" className="rounded-2xl border bg-white p-6 font-bold">Orders</Link><form action={async () => { "use server"; const client = await createServerSupabaseClient(); await client.auth.signOut(); redirect("/"); }} className="rounded-2xl border bg-white p-6"><button className="font-bold text-rose-700">Sign out</button></form></div></main></>; }
