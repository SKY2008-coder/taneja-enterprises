import Link from "next/link";
import { redirect } from "next/navigation";
import { getCustomerOrders } from "@/lib/supabase/storefront";
import { StorefrontHeader } from "@/components/storefront";
export default async function CustomerOrdersPage() { const orders = await getCustomerOrders(); if (!orders) redirect("/login?next=/account/orders"); return <><StorefrontHeader /><main className="mx-auto max-w-4xl px-4 py-8"><h1 className="text-3xl font-black">Your orders</h1>{orders.length ? <div className="mt-6 space-y-3">{orders.map((order) => <Link key={order.id} href={`/account/orders/${order.id}`} className="flex flex-wrap items-center justify-between gap-3 rounded-2xl border bg-white p-5"><div><p className="font-bold">{order.order_number}</p><p className="mt-1 text-sm text-slate-500">{new Intl.DateTimeFormat("en-IN", { dateStyle: "medium" }).format(new Date(order.created_at))}</p></div><div className="text-right"><p className="font-black">₹{Number(order.total ?? 0).toLocaleString("en-IN")}</p><p className="text-sm text-slate-500">{order.order_status} · {order.payment_status}</p></div></Link>)}</div> : <p className="mt-6 rounded-2xl bg-slate-50 p-8 text-sm text-slate-500">No orders yet.</p>}</main></>; }
