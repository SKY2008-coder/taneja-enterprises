import type { MetadataRoute } from "next";
import { getActiveCategories, getPublishedProductSlugs } from "@/lib/supabase/storefront";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const [categories, productSlugs] = await Promise.all([getActiveCategories(), getPublishedProductSlugs()]); const base = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000"; return [{ url: base, changeFrequency: "daily" }, { url: `${base}/products`, changeFrequency: "daily" }, { url: `${base}/categories`, changeFrequency: "daily" }, ...categories.map((category) => ({ url: `${base}/category/${category.slug}`, changeFrequency: "daily" as const })), ...productSlugs.map((slug) => ({ url: `${base}/product/${slug}`, changeFrequency: "daily" as const }))]; }
